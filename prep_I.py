
def chrismass_tree (n,c):
    n_ast=1
    n_spazio=h-1
    for x in range (0,4) :
        print (n_spazio * " " , n_ast * c )
        n_ast=n_ast+2
        n_spazio=n_spazio-1
altezza=int(input("dimmi l'altezza"))
char=input ("dimmi il carattere")
chrismass_tree(altezza,charv)

